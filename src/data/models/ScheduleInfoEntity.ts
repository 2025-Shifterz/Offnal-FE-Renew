export interface ScheduleInfoEntity {
  D: { startTime: string | null; duration: string | null }
  E: { startTime: string | null; duration: string | null }
  N: { startTime: string | null; duration: string | null }
}
